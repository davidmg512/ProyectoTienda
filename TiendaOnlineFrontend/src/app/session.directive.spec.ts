import { SessionDirective } from './session.directive';

describe('SessionDirective', () => {
  it('should create an instance', () => {
    const directive = new SessionDirective();
    expect(directive).toBeTruthy();
  });
});
